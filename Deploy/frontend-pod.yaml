apiVersion: v1
kind: Pod
metadata:
  name: frontend-pod
  labels:            
    app: frontend 
spec:
  containers:
  - name: frontend
    image: buraczydlo-frontend:latest
    imagePullPolicy: Never
    ports: 
    - containerPort: 80